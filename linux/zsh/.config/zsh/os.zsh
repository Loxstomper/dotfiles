# Linux (Arch / Omarchy) shell setup — sourced early by ~/.zshrc.
# Paths below are the pacman defaults; adjust if plugins come from elsewhere.
ZSH_SITE_FUNCS="/usr/share/zsh/site-functions"
ZSH_PLUGIN_DIR="/usr/share/zsh/plugins"
